generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = "mysql://0oio43q82mdoozlew75m:pscale_pw_cz2C6MwXLlMOcKtKs3CCewgh3kjht6DbgBrbyX0JgRW@aws.connect.psdb.cloud/todo_cli?sslaccept=strict"
  // url = env("DATABASE_URL")
  relationMode = "prisma"
}

model Todo{
  id        Int      @id @default(autoincrement())
  title     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  completed Boolean  @default(false)
}
